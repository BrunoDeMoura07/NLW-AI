import { pipeline } from '@xenova/transformers' 

import { summaryExemple } from './utils/summary.js'

export async function summarize (text) {
    try {
        
    } catch (error) {
        
    }
}